# cmux-browser — live board

Legend: ✅ done · 🎯 active · 📋 queued · 🔬 investigating · ⛔ blocked

_Started 2026-06-10. Decision (Lawrence): new dedicated repo; goal = Ghostty renders + custom UI + Chrome web tabs/omnibar still work; build both tracks in parallel. OWL stays untouched on its own repo._

## Done
- ✅ **CMD-W OMNIBOX-FOCUS CRASH FIXED (2026-07-14).** Dogfood exposed a real Chromium CHECK in `TabModel::IsPinned`: closing a web tab while its per-pane omnibox was focused destroyed/blured `LocationBarView` only after `TabStripModel` had detached the tab's owning model, so the blur callback reached `SearchTabHelper` with an ownerless tab. `OnTabWillBeRemoved` now clears focus while ownership is still valid. A permanent `CMUX_CMDW_SELFTEST` reproduces the exact ordering (focus omnibox → close web tab) and passed on both the builder binary and the locally deployed/signed app.
- ✅ **WORKSPACE = REAL CHROMIUM BROWSER REFACTOR COMPLETE (2026-07-14).** Every cmux workspace now owns a real Chromium `Browser` through a production `CmuxBrowserWindow`; its `TabStripModel` is authoritative for web tabs while cmux panes only project those tabs spatially. Native tab/window creation, activation, ordering, same-window moves, cross-window moves, and removal were exercised end-to-end, including a OneTab-style collapse and `windows.create({focused:false})`. Extension actions use Chromium's `ToolbarActionView` / desktop action delegate path with per-Browser containers; an in-process regression drives the actual toolbar mouse handlers and verifies first-click-open / second-click-close for uBlock Origin. The obsolete Browser-less shim is deleted, browser shutdown is synchronized with `BrowserList`, and custom-window safety seams for omnibox, fullscreen, side panel, and teardown are covered. Release build/deploy and codesign passed; host suites: 6/6, 5,228 checks. Architecture and remaining frame-UI boundaries are documented in `docs/extensions-findings.md`.
- ✅ **PERF + BUILDER-HYGIENE WAVE LANDED (2026-07-09, fable/perf-cleanups + fable/builder-hygiene).** (1) Offscreen compositor: ClientSharedImages now cached per IOSurface (ghostty triple-buffers → steady state reuses 3 via BackingWasExternallyUpdated, was CreateSharedImage per frame); IOSurfaceGetID-validated against same-address realloc, release callbacks matched by image identity (killed a latent DCHECK-fatal underflow). (2) Linux terminal fully demand-driven: 16ms poll cadence GONE; judge caught that ghostty's app-tick mailbox drain only DISPATCHES a render action (does not draw — GTK services it, we didn't), so ActionCb now routes GHOSTTY_ACTION_RENDER → pane's visibility-gated draw; 250ms net only for dropped .instant mailbox pushes; linux TU source-verified only — watch typing-echo latency next linux run. (3) Builder hygiene: apply.sh now fully idempotent (the BUILD.gn double-insert trap is guarded — no more reset dance except after editing the source lists); build.sh captures autoninja's real exit code (CMUX_BUILD_OK/FAILED; the grep|tail pipeline had produced 3 false-green builds; also: builder zsh reserves `status`); sync.sh --deletes inside cmux_term/ (3 stale niri_model leftovers purged). Both apply paths + no-op rebuild tested live on the builder. (4) Repo pruned: 31 landed branches deleted local+remote (remote now main + integration only), 6 diverged branches archive-tagged (archive/*) then deleted, 10 dirty worktrees left untouched for Lawrence triage (browserpane hosttests input legacy linuxpane macterm model rail tabstrip views).
- ✅ **THREADING AUDIT FIXES LANDED (2026-07-09, branch fable/threading-fixes-2).** Full overlay threading audit (docs/threading-findings.md) → all 12 findings closed. UI thread no longer does ANY config file IO: rail/layout/keymap/ghostty-theme loads via ThreadPool PostTaskAndReplyWithResult (weak-bound apply; rail apply ordering made deterministic via RailDelegate::OnRailConfigLoaded after a judge-caught double-load race), saves via NoDestructor'd ImportantFileWriter on a BLOCK_SHUTDOWN sequence; every ScopedAllowBlockingForTesting gone. CmuxRail::CloseRow posted-Unretained UAF fixed (weak ptr). Linux: g_ui_runner raw global → leaked userdata runner cell (happens-before documented); shared ticker gated on GetWidget()&&IsDrawn() (no 60Hz burn for hidden workspaces); close_surface_cb VERIFIED app-thread-only in the ghostty fork (all Surface.close() callers run under app tick / surface_key) and now DCHECK-asserted. Offscreen compositor: per-frame ClientSharedImage now owned by its viz::ReleaseCallback (+release sync token) instead of DoNothing+latest-only. GCD UI→UI deferrals → UI task runner (mac TUs are ARC — builder taught us: base::BindOnce(^{...}) block, no manual retain). libghostty drain contract VERIFIED in the fork: ghostty_surface_free JOINS renderer+io threads synchronously (App.closeSurface → Surface.deinit), documented in cmux_ghostty.h with rebase warning. 5 codex rounds, 2 judge REVISEs (both real: dcheck-build SequenceChecker& bind + the config double-load race). Ops lesson recorded in memory: fingerprint the builder's overlay base before sync.sh (first sync this cycle clobbered wave1's newer overlay and broke the build via the hand-patched extension files).
- ✅ **ENTRANCE ANIMATION + STRIP/TILED TOGGLE + ZERO-PADDING FULL-BLEED DEFAULTS SHIPPED (2026-07-08).** (1) New user-created columns/panes GROW IN through the shared EaseOutExpo animator (seeded collapsed at the target edge matching the glide direction) instead of popping to full size; startup seeding, workspace switches, drag re-parents stay instant (judge caught the startup terminal column animating — fixed via a user_action param). (2) `workspace.toggleLayout` (mod+shift+m): per-workspace kStrip (niri strip, math byte-identical) ↔ kTiled (columns share the viewport proportionally, no scroll; in-column bonsplit unchanged); columns glide between arrangements; CycleColumnWidth falls out as share adjustment in tiled. (3) Defaults: gap=0 margin=0 (seeded pre-JSON-load, explicit values win, knobs persisted so they're discoverable); a new workspace's first column seeds fraction 1.0 → full-bleed initial pane. ResolveDropTarget now sums real per-column widths (fixes a latent mixed-width bug). 3 judge rounds; host suites 6/6 (+model/layout/keymap checks); gate green incl. CMUX_ANIM_SELFTEST mid-flight motion. **Builder hygiene this cycle:** apply.sh double-insert trap re-hit (reset procedure now: full `git checkout -- .` + verify `grep -c 'cmux cross-platform'`==1); `cmux_theme_ghostty.cc` was registered for mac ONLY via a builder-local hand-edit — now committed in apply.sh's xplat list (third such drift captured). The two live sessions clobber each other's builder syncs — merge `fable/wave1` into integration before its next build.
- ✅ **Cmd+/−/0 ZOOM + THE GENERAL SHORTCUT FIX SHIPPED (2026-07-08).** Root cause of "not all shortcuts work": `CmuxBrowserSurface` implemented neither `PreHandleKeyboardEvent` nor `HandleKeyboardEvent`, so keys the renderer didn't consume died with no re-dispatch (upstream BrowserView's `UnhandledKeyboardEventHandler` pattern) — only the mac NSEvent monitor's chords worked, and only on mac. Now: HandleKeyboardEvent re-dispatches into the FocusManager; PreHandleKeyboardEvent runs RESERVED keymap chords pre-renderer with real auto-repeat state; keymap accelerators register on mac too (monitor swallows its matches first — no double-fire, traced 3 layers); `zoom.in/out/reset` (`mod+=`/`plus`/`-`/`0`) call the same `zoom::PageZoom::Zoom` as the 3-dot menu, `!terminalFocused`-gated so Ghostty keeps native font-size keys. Judged 2 rounds; keymap parser/when-clause tests added (host suite 6/6). Landed on the `fable/wave1` superset (= review-fix wave + integration tip 804c52d incl. ext-startup/webstore-ui) — plus two committed builder-drift fixes the last webstore build had only as builder-local edits (`~CmuxExtensionsContainer` override; `ResolvePuzzleAnchor` non-const for the builder's const `ViewTracker::view()`). Gate green incl. the new ext assertions (post-install dialog, browser-stays-hidden, placeholder slots); no NOTREACHED this run. NOTE for the parallel session: merge `fable/wave1` before the next build — the committed integration tip alone no longer compiles on the builder without these two fixes.
- ✅ **REVIEW-FIX WAVE × FEATURE TRUNK MERGED + SHIPPED (2026-07-08).** A 3-axis code review (model core 7/10, Views 6.5/10, platform 7/10 vs upstream ≈7-8) produced ~30 findings; 10 parallel codex coders (one isolated worktree + disjoint file set each, fable-judge gate per task, 3 needed revision rounds) fixed them all: CmuxTab self-deletion UAF (deferred select/new-tab), dangling `g_strip`/keep-alive on user window-close (WidgetObserver teardown, both orders exactly-once), closed terminal panes now kill their shell (bounded deferred NSView release → `ghostty_surface_free`), offscreen dtor race closed (`stopped_` under `frame_lock_` + libghostty render-thread-join guarantee, verified in the ghostty fork source), browser-pane omnibox-client dangle + all `Unretained` binds → WeakPtr, removable key monitors, `MoveTab` same-pane reorder + ">=1 tab per pane" invariant (incl. `kInvalidId` resolution), shared-ticker + `SkPixmap` row swizzle on linux/win pane, legacy AppKit strip (~700 lines) and `niri_model` deleted, `scripts/run-host-tests.sh` gates the pure-model tests. Then merged with the parallel session's 66-commit feature trunk (DnD/reorder, keymap, ghostty theming, extensions, titlebar): trunk architecture kept, every safety fix re-applied semantically, superseded refactors dropped (selftest extraction, chord dedup → keymap), test suites unioned (window_model 26 tests / 211 checks); merge judged in 3 fable-judge rounds. Gates green: host tests ×5, theme selftest, devtools full lifecycle, cmux:// mapping. Branch `fable/wave1` (merge commit + fixups); integration can fast-forward.
- ✅ **EXTENSIONS + WINDOW CHROME + TAB BEAUTY land together (2026-07-04, branch fable/integration; feature branches fable/extensions-preinstall, fable/titlebar-sidebar, fable/tab-beauty).** (1) **Full uBlock Origin (MV2) preinstalled**: scripts/fetch-extensions.sh pulls the CWS crx, verifies+injects the real uBO key (id pinned cjpalhdlnbpafiamejdnhcphjbkeiagm); deploy.sh bundles into Resources/cmux-extensions; startup loads via UnpackedInstaller; MV2 enablement mirrors the Helium fork (apply.sh patches: IsAllowedManifestVersion, MV2-policy exemption, ShouldDisableLegacyExtensions=false, no-nag) — verified live: "loaded bundled uBlock Origin … version=1.72.0 manifest_version=2". (2) **Bitwarden user-land install**: external-extensions JSON (CWS update URL) written off-thread to the profile + CheckForExternalUpdates rescan; mac's prompt_for_external_extensions gate (external installs land disabled, enable-bubble needs a Browser) resolved by a watcher scoped to the Bitwarden id that acknowledges+enables it — verified live: installed from the store and auto-enabled ~60s after first launch. Action strip observes ExtensionRegistry (installs appear without restart); Web Store install prompt anchored to the initiating WebContents. (3) **Custom mac titlebar**: full-size content + transparent hidden titlebar, traffic lights over the rail, rail header = native window-drag region (performWindowDragWithEvent, live NSEvent); "+" new-workspace moved to the top-left header; Cmd-B/Ctrl-B animated sidebar toggle (persisted, drag-cancel-safe); ~/.cmux_layout.json (RailConfig pattern) for rail width/gaps/margins/column sizing + demo sliders; Arc-style rail polish (pills, color dots, indent guides). (4) **Tab strip beauty**: favicons (ContentFaviconDriver, cached across surface reparents), loading Throbbers (SurfaceTab.loading finally wired via SetTabLoading), hover + close-on-hover + selected top accent, middle-click close, right-click context menu (Close/Close Others/New Tab Right/Move to New Column/Split With Tab) reusing the DnD commit paths, even-shrink overflow. Process notes: three fable loops (GPT-5.5 xhigh coder + Fable judge), judges caught 2 compile-blockers host tests can't see (extension_urls namespace, ScopedAllowBlocking friend-gating → ThreadPool rewrite), plus LabelButton::label() being protected → SetTextSubpixelRenderingEnabled; ALSO learned: `autoninja | tail` masks build failures — two "green" builds were stale binaries; always pipefail + check "Build Succeeded" + strings-check the deployed binary. Final gate on the merged tree: build green, CMUX_DND_SELFTEST 6/6 + SURVIVED, uBO MV2 loaded enabled, Bitwarden installed+enabled, zero crashes over 110s. FOLLOW-UP (2026-07-04 PM): (a) dogfood bug "blue drop highlight behind the ghostty terminal" root-caused (native NSViews composite above ALL in-window Views layers on mac — no in-widget overlay can cover a surface) and fixed on branch fable/dnd-overlay-widget: ghost + highlight now live in a per-drag transparent click-through TYPE_POPUP overlay widget (CLIENT_OWNS_WIDGET, kFloatingUIElement) — judge APPROVED round 1. (b) CROSS-PLATFORM PROVEN on all 3 OSes from the same integration tree: Linux (restored cmux-linux-chrome snapshot) — chrome + views_examples build green, CMUX_DND_SELFTEST 6/6 on Linux, full-app screenshot w/ 2 live ghostty terminals, and a LIVE xdotool drag mid-flight screenshot showing the ghost pill + translucent center-drop highlight rendering ABOVE the terminal (plus pixel-verified left-half/bottom-half edge-zone rects; a "full-pane left highlight" scare was geometry — niri columns extend past the viewport so the half-rect covers ~85% of the visible pane); Windows (cmux-win) — views_examples + FULL chrome build green (one legit platform gate: external-extensions JSON helpers compiled out on Windows, which uses the registry) and a full-app screenshot (rail header, favicon pills, stacked split, live ghostty cmd.exe); mac — rebuilt + 6/6 selftest + uBO/Bitwarden re-verified on the final tree. Windows box note: C:\capture_chrome.ps1 is now a capture-only script targeting the window TITLED cmux (views_examples shares the Chrome_WidgetWin class and hijacked the old biggest-window heuristic).
- ✅ **TAB/PANE DRAG-AND-DROP + per-pane terminal tabs + spatial focus (2026-07-04, branch fable/tab-dnd-term-tabs-focus-fix).** Bonsplit-style DnD in the real renderer: drag a tab to reorder in-strip (animated insertion gap + indicator), into another pane's strip or body-center (moves the tab, surface REPARENTED not recreated), onto a pane edge (25%/80px bands → split carrying the tab, left/top insert first), or into a column gap / strip edge (niri: tab becomes its own new column); drag a whole pane by its strip's trailing space and merge it into another pane as tabs; ghost pill + animated accent drop-zone highlight; source pane auto-closes when emptied. Model ops (`ReorderTab`/`SplitPaneWithTab`/`MoveTabToNewColumn`/`MergePaneInto` + pane moves) and `ResolveDropTarget` are pure + host-tested (window_model_test 164, window_layout_test 130 checks). NEW `>_` button beside `+` makes a terminal tab in that pane. FIXED the Cmd-D/Cmd-Shift-D focus-skip: `SpatialNeighborPane` (geometric, in window_layout) replaces order-based NeighborPane for Cmd-Ctrl-hjkl / Cmd-Opt-arrows — left/right now reach in-column side-by-side siblings before jumping columns. Fable loop: 3 codex rounds, judge-verified (friend/compile fix, mid-drag pane-destruction cancel guard, gap-anim jitter); dogfood crash fixed post-approval (label.cc subpixel DCHECK under the gap-anim's non-opaque layers → grayscale AA like the rail). Verified: mac chrome + views_examples build green, deployed, CMUX_DND_SELFTEST 6/6 PASS + SURVIVED, 90s interactive run clean.
- ✅ **ALL KNOWN CRASHES FIXED (2026-07-03, second pass).** (1) **Shutdown FATAL `observers_.empty()` gone**: pane tab helpers (BookmarkTabHelper via extensions::TabHelper) observe profile KeyedServices; the leaked process-lifetime widget kept them registered past BookmarkModel teardown. `CloseViewsWebWindow()` now destroys the niri window synchronously at shutdown — hooked at `chrome::AddClosingAllBrowsersCallback` (fires in AttemptExitInternal on every exit path incl. SIGTERM), with AppTerminating + PostMainMessageLoopRun backstops — and releases the now-owned BROWSER ScopedKeepAlive (`HoldViewsKeepAlive`) so the process exits (mac applicationWillTerminate CHECKs no BROWSER keep-alive remains). Verified: SIGTERM → clean exit, all processes gone in 2s, zero crashes. (2) **Blank terminal pane fixed** (Lawrence's screenshot): launching while the display is asleep/locked → `CGGetActiveDisplayList`=0 → libghostty CVDisplayLink creation fails (-6661) → `ghostty_surface_new` null → pane permanently blank. Now retries every 2s until the display is back, then pushes size/focus; testable with `CMUX_GHOSTTY_FAIL_FIRST=n`. (3) run.sh: `CMUX_NO_LLDB=1` exec-in-pipeline fell through and double-launched (a second lldb instance); lldb now passes SIGTERM/SIGINT/SIGHUP through so graceful shutdown is testable under the crash-capturing debugger. Devtools lifecycle selftest re-verified green after all changes.
- ✅ **CRASH FIXED + feature crash-tested under lldb/CDP (2026-07-03).** Extended CMUX_DEVTOOLS_SELFTEST (open docked → undock → toggle-close → toggle during window-close race → docked flip → undock again → close pane with undocked window open) caught an EXC_BAD_ACCESS: **DevToolsUIBindings::SetDelegate takes unique_ptr OWNERSHIP of its delegate** (upstream's DevToolsWindow is owned that way) — we handed it `this` (a views-hierarchy-owned view), so pane close destroyed the surface twice (~CmuxBrowserSurface → ~WebContents → ~DevToolsUIBindings → delete delegate_ == the surface again). Fix: bindings own a small `CmuxDevToolsDelegateProxy` holding a WeakPtr; the surface invalidates weak ptrs first in its dtor. All selftest steps green under lldb, zero crashes. Also live-verified over CDP (--remote-debugging-port + probe extension with an options page): `developerPrivate.showOptions` → browser survives, options page opens in the pane (the apply.sh guard works); `management.setEnabled` off/on works; cmux://extensions AND cmux://version load the real WebUI (mapping is generic) with virtual URL + omnibox showing cmux://.
- ✅ **DevTools close/undock controls + cmux://extensions (2026-07-02).** (1) The docked inspector (undocked-mode frontend = no built-in chrome) now sits under a slim header row: "DevTools" label + open-in-new-window button + close X. (2) Undock (button, or `UndockDevTools` through surface/pane/controller seams) hands the devtools WebContents to a fresh WebView in its own top-level views::Widget — bindings/agent attachment untouched; closing that window (native X / our X / ToggleDevTools) re-attaches it to the hidden docked panel, so window-close == devtools closed and the next toggle shows it docked. Pane dtor CloseNow()s the window first with the WebView detached. (3) cmux:// maps to chrome:// via a BrowserURLHandler registered in ShowViewsWebWindow (same mechanism as upstream's about:→chrome:) + an apply.sh IsHandledProtocol patch so typed cmux:// input classifies as URL: cmux://extensions loads the real chrome://extensions WebUI while omnibox + virtual URL keep displaying cmux://extensions (CMUX_CMUXURL_SELFTEST verified: real=chrome://extensions/ title=Extensions omnibox=cmux://extensions). (4) The two chrome://extensions crash paths from docs/extensions-findings.md are guarded in apply.sh: developerPrivate.showOptions `CHECK(browser)` → navigate the sender pane to the options URL; post-install success toast FindOrCreateVisibleBrowser (stray tabbed window) → skipped when no tabbed Browser exists. CMUX_DEVTOOLS_SELFTEST now drives the full lifecycle (open docked → undock → toggle-closes window → re-shows docked) — green. **Known PRE-EXISTING (not this change, repros on a plain run):** shutdown FATAL `observer_list.h Check failed: observers_.empty()` — extensions::TabHelper (attached in AddBrowserSurface) creates BookmarkTabHelper whose BookmarkModel observer is still registered when the KeyedService tears down before our leaked process-lifetime widget.
- ✅ **Tabs + splits VERIFIED on all 3 platforms (2026-07-02).** CMUX_DEMO_LAYOUT=1 builds a deterministic showcase through the real controller paths (second tab in pane 1, side-by-side split, stacked split) — screenshots in dist/screenshots/cmux_{mac,linux,windows}_tabs_splits.png show the identical layout everywhere: per-pane tab strips (2 tabs + "+"), bonsplit-style h+v splits inside column 1, focused split with blue border and its own omnibox, live ghostty terminal column (zsh on mac, bash on cmux-linux4, cmd.exe on cmux-win). Same cross-platform Views C++ on macOS (AppKit host), Linux (Xvfb/SwiftShader) and Windows (llvmpipe).
- ✅ **TABS + IN-COLUMN SPLITS — the cmux layout model lands (2026-07-02).** Hierarchy is now workspace → columns (niri strip) → per-column bonsplit tree of PANES → per-pane TABS of SURFACES (web page or terminal). Pure model: `Workspace.layout` replaced by `std::vector<LayoutNode> columns` with `AddColumn`/`ColumnIndexOf`/`ColumnCountOf`/`NeighborPane(Direction)`; `SplitPane` splits in place inside its column; `ClosePane` collapses in-column, removes the column when it was the lone pane, refuses on the workspace's last pane. `ComputeStripLayout` keeps today's column-scroll math verbatim (keyed by focused pane's column) and tiles each column's split tree inside its fixed-width strip (`StripLayout.dividers` added). Renderer split into `CmuxSurface` (one tab's content: `cmux_browser_pane.cc` = web incl. omnibox/DevTools; `cmux_terminal_pane.mm`/`_linux.cc` = ghostty, the _linux file also serving Windows) + `CmuxPaneView` (tab strip `cmux_tab_strip.cc` over a surface stack; selected surface visible) implementing the old `CmuxPane` interface toward strip/focus code. Tab titles live from WebContents (`TitleWasSet`/NavigationEntry, "Terminal" for shells). New chords (mac Cmd / other Ctrl): T new web tab, Shift-T new terminal tab, D split side-by-side, Shift-D split stacked, N new column, W close tab (pane/column when last, workspace's last pane refuses), Shift-[ / Shift-] cycle tabs, arrows/Cmd-Ctrl-hjkl now traverse panes (Up/Down within column). Host tests: window_model_test 110 checks, window_layout_test 93 checks, niri_model_test green. Mac + Windows chrome build green; Windows verified by scheduled-task drive (screenshots: 2-tab strip, tab close, [pane | stacked splits] in one column, focus traversal + strip scroll). Linux build box (cmux-linux3) no longer exists — unverified there, but its terminal pane source is exactly what Windows built/ran. Windows terminal pane still renders black (pre-existing ghostty-on-win gap, not this change). Extensions research landed in `docs/extensions-findings.md` (core uBlock/DNR/content-script machinery already works Browser-less; P0 = action strip + popup host + 2 crash guards, one of which — WebContentsModalDialogManager attach — shipped in this pass). Design doc: `docs/splits-and-tabs.md`.
- ✅ **FIXED: undefined-pixel rendering artifacts in the strip background (2026-07-01).** The gutters between columns / top-bottom margins / regions panes animate away from rendered as garbage (white fill + blue focus-border trails + a stale omnibox-popup fragment on Windows; black bands on Windows) — visible on ALL THREE platforms in the same places. Root cause: `content_` (the pane-clipping container in cmux_views.cc, added for the rail-clip fix) is `SetPaintToLayer()` — opaque by default — but painted NO background, so every pixel not covered by a pane was undefined; the compositor filled it with its "safe" color and stale fragments never repainted. Fix: paint `kStripBackground` on `content_` itself (every pixel defined, layer stays opaque — no blending cost) + an INVARIANT comment: any SetPaintToLayer() view must paint its full bounds or SetFillsBoundsOpaquely(false). Audited all other cmux layer sites: rail containers already used FillsBoundsOpaquely(false); rail + compositor pane paint full backgrounds. Verified with before/after screen recordings on macOS + Linux + Windows (dist/recordings/cmux_{mac,linux,windows}_{clean,fixed}.mp4): gutters now uniform strip background, no trails, no fragments.
- ✅ **Code-quality pass: chromium-style cleanup across cmux_term (2026-07-01).** (1) FIXED the known WebContents leak on column close: CmuxBrowserPane now OWNS its page WebContents (unique_ptr through AddBrowserPane; dtor detaches WebView + delegate before destroying). (2) cmux_views.cc and cmux_browser_pane.cc include lists pruned (~60 stale includes each, left from the pane split) and sorted. (3) File-private strip class in cmux_views.cc renamed CmuxStripView → CmuxWindowView (no more name collision with the public cmux_strip_view.h class). (4) RailConfig + JSON persistence split out of the 1.3k-line cmux_rail.cc into cmux_rail_config.{h,cc} (BUILD.gn + apply.sh updated). (5) Legacy AppKit path deduped: cmux_term_mac.mm now uses cmux::InstallEditMenu() + cmux::CmuxDemoURL() instead of its own copies (~180 lines gone). (6) OWL-named ghostty callbacks renamed, dead GhosttyVersionProbe/AddPane/E2EDriveFocusedNav removed, 40 seeded "Space NN" scroll-test workspaces removed. (7) window_layout_test.cc rewritten: the old NiriModel-parity assertions were stale (the strip deliberately keeps `gap` breathing room around the focused column) and failing at HEAD; now asserts the intended scroll-into-view invariants — 68 checks green, all three host tests green. (8) clang-format --style=Chromium over every cmux source; stale comments (renamed .mm files, wrong "mac-only" seam claims) fixed.
- ✅ **V3+V4 — Views niri strip: real-omnibox Chrome panes + Ghostty terminals (2026-06-12).** Delivery met (screenshot): one "cmux" window, a Chrome web pane (real LocationBarView omnibox) beside a live Ghostty terminal (real shell prompt), focused column blue-bordered. CmuxStripView (views::View) drives niri tiling via the extracted NiriModel; columns are CmuxBrowserPane (LocationBarView + WebView) or CmuxTerminalPane (Ghostty NSView in views::NativeViewHost). Cmd-T/Cmd-N create terminal/Chrome columns, Cmd-W close, Cmd-Ctrl-h/l + Cmd-Opt-arrows move focus (scroll-into-view), BoundsAnimator slides, Cmd-L focuses the real omnibox. Two crashes fixed: process self-shutdown -> ScopedKeepAlive(BROWSER); clean teardown attaching Ghostty NSView in ctor -> defer NativeViewHost::Attach to AddedToWidget. Chrome's stray startup window suppressed via --no-startup-window (CMUX_VIEWS). Awaiting Lawrence dogfood of focus/scroll/create. Known: closing a column leaks its WebContents (quit-time SiteProcessCountTracker DCHECK); click-to-focus not wired yet.
- ✅ **V2.5 — real omnibox NAVIGATION wired (Chrome's code, no Browser) (2026-06-12).** Two surgical fork patches (apply.sh, idempotent, default behavior unchanged for all other Chrome windows): (1) LocationBarView gets a `virtual CreateOmniboxClient()` template-method seam (default still returns ChromeOmniboxClient); (2) ChromeOmniboxClient drops `final`. Our CmuxLocationBarView overrides CreateOmniboxClient to return CmuxOmniboxClient (subclass of ChromeOmniboxClient, browser=nullptr) which overrides ONLY OnAutocompleteAccept to LoadURL on our own WebContents — so typed-URL navigation + Google-search both drive our pane, while inheriting all of Chrome's autocomplete/suggestion wiring. Builds green, omnibox still renders (screenshot), 4 renderers, no crash. Navigation/suggestions are wired; live keystroke confirmation pending (not safe to synthesize global keys on a shared Mac). NEXT: V3 niri strip of these panes (drive with NiriModel) + V4 terminal via NativeViewHost.
- ✅ **V2 — REAL Chrome omnibox renders (LocationBarView), no custom omnibar (2026-06-12).** CmuxBrowserPane (a views::View) is the LocationBarView::Delegate + ChromeLocationBarModelDelegate + CommandUpdaterDelegate; owns a LocationBarModelImpl + CommandUpdaterImpl; builds `LocationBarView(nullptr, profile, &command_updater_, this, false)` + a views::WebView in a vertical box and calls location_bar_view_->Init() from AddedToWidget() (Init DCHECKs GetWidget). Attaches the explicit tab-helper set (security/passwords/content-settings/etc.) the omnibox icons look up. Verified by screenshot: window shows Chrome's actual location bar (URL + real security chip) above the live page; 4 renderers, no crash. Suggestions work (need only the Profile). NEXT V2.5: typed-URL navigation via a CreateOmniboxClient seam in LocationBarView + a CmuxOmniboxClient (ChromeOmniboxClient routes nav through a Browser we don't have).
- ✅ **V1 — Chromium Views render in our forked process (2026-06-12).** Top-level `views::Widget` (TYPE_WINDOW) hosting a `views::WebView` bound to a WebContents (example.com), shown from PostBrowserStart under `CMUX_VIEWS=1`. 5 renderers, no crash, boots clean. Proves the Views toolkit + WebContents embedding work in our binary — the prerequisite for hosting the REAL Chrome omnibox (LocationBarView). Gotchas solved: new code can't subclass WidgetDelegateView (friend-gated) → bare `views::WidgetDelegate` + SetContentsView, leaked for process lifetime; TYPE_WINDOW requires contents via delegate (not Widget::SetContentsView). Next: V2 add real LocationBarView (Browser-less, PresentationReceiverWindowView pattern; navigation via a CreateOmniboxClient seam).
- ✅ **Clean refactor: pure cmux::NiriModel extracted (cross-platform core) (2026-06-11).** All niri layout/focus/scroll math moved into a header+cc model (`niri_model.h/.cc`) with ZERO UI/platform deps: column order, focused index, scroll offset, column-width, frame geometry, scroll-into-view, hit-test, add/close/move. CmuxStripView is now a thin AppKit renderer that keeps a parallel native-view array and applies the model's frames/focus. Backed by a host-side unit test (`niri_model_test.cc`, 7 cases) that compiles+runs with a plain `c++` (no Chromium/gtest) — green. No behavior change (formulas preserved verbatim, app preflighted). This is step 1 of the cross-platform plan: the model is the write-once core; only the renderer is per-toolkit. Next milestone = port the renderer from AppKit to Chromium `views` (write-once UI on mac+Linux+Win) which also unlocks the real omnibox cheaply. Ghostty bounds platforms to mac+Linux.
- ✅ **C2.3 — Cmd-A/C/V/X/Z in web panes via a standard Edit menu (2026-06-11).** Chrome's own Edit menu validates against an absent Browser (greyed out); replaced with a responder-chain Edit menu so editing keys reach the focused web pane / terminal / URL field.
- ✅ **C2.2 — per-web-pane omnibar + focus restoration + nav UX (2026-06-11).** (a) Each web column carries its content::WebContents; focus changes route through WebContents::Focus() so navigating away from a focused DOM input and back restores the caret to the same field (was: bare makeFirstResponder, which blurred and lost it). (b) Native omnibar in each web column header (back/forward/reload + URL field; Enter navigates URL-vs-search; a WebContentsObserver keeps the URL live; Cmd-L focuses it). Not Chrome's Views omnibox (that needs a Browser/BrowserView we don't fork); native bar drives the same WebContents. (c) Global NSEvent key monitor makes Cmd-Ctrl-hjkl / Cmd-Opt-arrows work even from a focused web input (beats Chromium's renderer-first key handling); click-to-focus monitor; 0.22s slide animation on focus-scroll. Demo web column is a self-contained dogfood page (inputs/drag-drop/buttons/scroll). Preflight: window+demo+terminal+omnibar live, no crashes. Awaiting Lawrence dogfood.
- ✅ **C2 — niri scrollable tiling strip (paper WM) of mixed panes (2026-06-11).** AppKit (Obj-C++) horizontal column strip: each column is a native pane (real Chrome WebContents OR Ghostty terminal), columns lay out left→right, focused column scrolls into view (niri feel). Shortcuts: Cmd-T new terminal column, Cmd-N new web column, Cmd-W close, Cmd-Shift-[ / Cmd-Shift-] move focus. Launches with 1 web + 1 terminal column. Confirmed live (5 web renderers + 1 shell). UI built in C++/Obj-C++ AppKit (panes are native views; tiling = frame math). Awaiting Lawrence dogfood.
- ✅ **FIXED (was misdiagnosed as `-lc++`): intermittent startup hang was the macOS Keychain (2026-06-12).** Real stack: `BrowserProcessImpl::PreMainMessageLoopRun` → `os_crypt_async` → `crypto::apple::KeychainV2::SecKeychainSearchCopyNext` blocking on a Keychain prompt — the ad-hoc-signed dev build isn't recognized by the Keychain ACL (the earlier `userCanceledErr -128` was that prompt). Fix: `PreEarlyInitialization` appends `--use-mock-keychain` before os_crypt runs, so the dev build always boots (no real OS encryption — fine for a dev artifact). The `-lc++` link is NOT the cause; that diagnosis was wrong.
- ✅ **C1 — unified window: Chrome web pane + Ghostty terminal in ONE window (2026-06-11).** Our own NSWindow with an NSSplitView: left = real `content::WebContents` (created in-process, GetNativeView() embedded, loaded example.com, its own renderer); right = CmuxGhosttyTerminalView (live shell). Both confirmed live in the same browser pid (4 web renderers + a login/zsh child). Decision: build our OWN window/layout, NOT chrome's BrowserWindow/TabStripModel (WebContents-only, heaviest fork). Awaiting visual dogfood. Next: pane tree with tabs + dynamic splits (cmux-style).
- ✅ **B2 — real Ghostty terminal renders in the forked browser (2026-06-11).** From fork code, logs confirm: `ghostty app created` → `ghostty surface created (shell spawned)` → `opened native terminal surface window`, with the browser stable (3 web renderers) and a `login…/bin/zsh` shell running as a direct child of the browser process. libc++ ABI gap solved with `-lc++` (links libghostty's out-of-line libc++ refs from the system dylib; safe because chromium inlines its own libc++, nothing interposable). Startup deadlock was eager init in PostBrowserStart, fixed by deferring surface creation to a main-queue task. CmuxGhosttyTerminalView (cmux_ghostty.mm) owns app/surface lifecycle, Metal render via nsview, key/mouse forwarding. AWAITING visual dogfood (does text paint / is it interactive).
- ✅ **F0 baseline** — full `chrome` from our fork runs visibly as a real browser (perfect tabs/omnibar/resize/input). Builder branch `cmux-browser` off the manaflow chromium fork; warm `out/Release` reused.
- ✅ repo + build orchestration scaffold (overlay/patches/scripts, sync→apply→build).

## Active (parallel)
- ✅ **B0** — native AppKit surface window opens from fork code (`CMUX_TERM=1`) inside the chrome process; web tabs/omnibar untouched.
- ✅ **B1 (links + builds)** — prebuilt libghostty LINKS into the forked chrome framework and the binary builds + dylib-verifies. How (all in `scripts/apply.sh`): vendor GhosttyKit's fat `.a`; thin→arm64; `ld -r -all_load` merge (collapses the 1 dup member); `vtool` rewrite minos 13→12 (lld hard-errors on newer than chrome's 12); export only `_ghostty_*` so bundled simdutf/freetype/harfbuzz/zig-std/sentry go local (no dup-symbol vs chrome); repack; libc++ ABI gap (`std::mutex::~mutex()`) bridged by a mangled-symbol alias shim; link attached to the **chrome_framework** bundle (not the exe).
- ✅ **B1 RUNTIME PROVEN (allocator blocker SOLVED).** Embedding libghostty first caused a startup abort `Trying to load the allocator multiple times` (Chromium's allocator shim vs libghostty's Zig runtime). Fixed by disabling PartitionAlloc-as-malloc + allocator-shim + BackupRefPtr (gn args; `scripts/set-alloc-args.sh`). The forked Chromium now boots, runs real web tabs (3 renderers), and from fork code logged `libghostty version 1.3.2-HEAD-+ff6e126` (ghostty_info() ran in-process) + `opened native terminal surface window`. Tradeoff accepted for this fork: no PartitionAlloc hardening. (Headless ssh launch is a false pass — must dist + launch locally to test runtime.)
- 🎯 **B2 (was headline — no longer infra-blocked)** — render a real Ghostty surface into the B0 NSView: app init + Metal layer + runtime callbacks, porting `Ghostty.App.swift`/`SurfaceView_AppKit.swift`. Then B3 input, B4 terminal-as-tab.
- 🎯 **Track A — custom UI.** Entry point first: our command/affordance to open the terminal surface, web tabs + omnibar untouched. Then tab-strip/omnibar styling in `chrome/browser/ui/views`.

## libghostty materials (found on builder, unblocks B1)
- Static lib: `~/cmux-runners/parallel-xcodebuild-exp/cmux/ghostty/macos/GhosttyKit.xcframework/macos-arm64_x86_64/ghostty-internal.a`
- C API header: same dir `/Headers/ghostty.h`
- AppKit driver reference (how to init app + create/render a surface): `cmux/ghostty/macos/Sources/Ghostty/Ghostty.App.swift` and `Surface View/SurfaceView_AppKit.swift`
- B1 = a gn `static_library`/`config` wrapping the prebuilt `.a` + that Headers dir as an include path; no Zig toolchain needed (lib is prebuilt). Link into the `chrome` target (or a small intermediate). B2 = port the App.swift/SurfaceView init into cmux_term.mm to render into the surface NSView's layer.

## Queued
- 📋 libghostty gn target (wrap the prebuilt `.a` + Headers above).
- 📋 terminal-as-tab (TabStripModel parallel contents path) — the hard integration.
- 📋 decide replace-vs-coexist with OWL after Ghostty renders.
